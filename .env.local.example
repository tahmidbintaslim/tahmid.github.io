# SMTP Configuration for Contact Form
# For Gmail: Use App Password (not your regular password)
# 1. Enable 2-factor authentication on your Gmail account
# 2. Go to https://myaccount.google.com/apppasswords
# 3. Generate an App Password
# 4. Use that password here

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
CONTACT_EMAIL=your-email@gmail.com

# Vercel Blob Storage Configuration (for file uploads)
# Get this from: Vercel Dashboard → Project → Settings → Blob Storage
BLOB_READ_WRITE_TOKEN=your_blob_token_here

# Vercel KV (Redis) Configuration (for feedback storage)
# Get this from: Vercel Dashboard → Project → Settings → KV Database
KV_REST_API_URL=https://your-kv-instance.kv.vercel.sh
KV_REST_API_TOKEN=your_kv_token_here

# SEO Configuration
# Google Search Console verification code (optional but recommended)
# Generate at: https://search.google.com/search-console
GOOGLE_VERIFICATION_CODE=

# News Widget Configuration
# Optional: Get a free API key from https://gnews.io/
# The news widget will work with fallback RSS feeds if this is not configured
NEXT_PUBLIC_GNEWS_API_KEY=

# Security & Admin Configuration
# Generate: openssl rand -hex 32
ADMIN_TOKEN=

# Webhook Notifications (optional)
# Discord webhook for feedback notifications
DISCORD_WEBHOOK_URL=

# Slack webhook for feedback notifications
SLACK_WEBHOOK_URL=
